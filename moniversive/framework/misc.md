# `misc`

Family: **`mis_lang`** — Developer Experience & Language Tooling

**MIS kernel compiler** — prove invariants, letter-hash, emit execution graph. **Not Python.**

```bash
bin/misc moniversive/framework/misc.mis --check --compact-letters
bin/misc path.mis --check --compact-letters
make misc-build
```

Module: `moniversive/framework/misc.mis` · Binary: `bin/misc`
