# `mis_dbg`

Family: **`mis_lang`** — Developer Experience & Language Tooling

Thermal MSR monitor, register-spill diagnostics, shadow-stack checks

Author under `moniversive/` as `.mis`. Resolve via `clrty_getMisCodeIndex`. Prefer branchless `mis.select` / `mis.invariant` over exceptions.

```bash
bin/misc moniversive/framework/mis_lang.mis --check --compact-letters
```
