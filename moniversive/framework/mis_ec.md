# `mis_ec`

Family: **`mis_crypto`** — Moniversive Cryptographic Primitives

Field arithmetic Fp / Fp251 / Goldilocks; MSM Pippenger; BN254 / BLS12-381

Author under `moniversive/` as `.mis`. Resolve via `clrty_getMisCodeIndex`. Prefer branchless `mis.select` / `mis.invariant` over exceptions.

```bash
bin/misc moniversive/framework/mis_crypto.mis --check --compact-letters
```
