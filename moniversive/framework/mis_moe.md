# `mis_moe`

Family: **`mis_ml`** — High-Level Invariant AI & Tensor Engine

Top-K MoE routing kernels and structural pruning bitmasks

Author under `moniversive/` as `.mis`. Resolve via `clrty_getMisCodeIndex`. Prefer branchless `mis.select` / `mis.invariant` over exceptions.

```bash
bin/misc moniversive/framework/mis_ml.mis --check --compact-letters
```
