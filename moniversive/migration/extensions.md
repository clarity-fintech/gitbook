# Extension map

| Old | New |
|-----|-----|
| `.clrty` | `.clrty` |
| `.crty` (interim) | `.clrty` |
| `foundry.toml` | `clrty.toml` |
| `solc` / `forge` | `clrtyc` |

**Policy:** nothing ending in `.clrty` remains in the canonical CLRTY monorepo trees after purge. Archives under `export/` are converted or deleted with the same rule.
