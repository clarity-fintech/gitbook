# Contribute

1. Author through MIS filter (`.clrty` + EMBED 3..=6).  
2. Add a `mis_code_index.json` entry.  
3. Extend continuous catalog only via documented bands.  
4. Run `make mis-ml-check`.  
5. Do not change live `SLOT_MS` or L01 fee bps unless explicitly requested.
