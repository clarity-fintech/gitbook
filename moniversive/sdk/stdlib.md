# SDK & stdlib

Path: `languages/mis-ml/stdlib/std.clrty`

| Module | Replaces |
|--------|----------|
| `std::token` | IERC20 / SafeERC20 |
| `std::access` | Ownable / timelock gates |
| `std::pause` | Pausable |

Import:

```clrty
use std::token;
use std::access;
use std::pause;
```
