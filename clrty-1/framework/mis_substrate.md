# `mis_substrate`

Family: **`mis_chain`** — Blockchain & Cross-Chain Interoperability

SCALE compact integers + SR25519 verify

Author under `moniversive/` as `.mis`. Resolve via `clrty_getMisCodeIndex`. Prefer branchless `mis.select` / `mis.invariant` over exceptions.

```bash
bin/misc moniversive/framework/mis_chain.mis --check --compact-letters
```
